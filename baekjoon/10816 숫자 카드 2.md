https://www.acmicpc.net/problem/10816

```java
import java.util.*;
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));
        PrintWriter out = new PrintWriter(System.out);

        int cardCount = Integer.parseInt(reader.readLine());
        int[] cards = new int[cardCount];
        
        StringTokenizer in = new StringTokenizer(reader.readLine());
        for (int i = Integer.parseInt(in.nextToken()); i < cardCount; i++) {
            cards[i] = Integer.parseInt(in.nextToken());
        }
        
        int resultCount = Integer.parseInt(reader.readLine());
        // cardnum, count
        Map<Integer, Integer> cardStore = new HashMap<>();
        
        in = new StringTokenizer();
        
        for (int i = 0; i < resultcount; i++) {
            int j = Integer.parseInt(in.nextToken());
            cardStore.put(j, 0);
        }
        
        for (int i = 0; i < cardCount; i++) {
            cardStore.replace(cards[i], cardStore.get(cards[i]) + 1);
        }
        
        for (int i : cards) {
            out.print(card);
            out.print(' ');
        }
        
        out.flush();
    }
}
```
